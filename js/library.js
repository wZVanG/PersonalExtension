function AllPlugins(){	var self = this;	this.PluginName = "none";	this.matches = {};	this.injecteds = {};		this.none = function(){		$$("Nothing...")	}	this.add = function(id,regex,fn,is_injected){		this[id] = fn;		this.matches[id] = regex;		this.injecteds[id] = !!is_injected;	};	this.get = function(url){		var PluginName = "none";		$.each(self.matches,function(id,reg){			if(url.match(reg)){				PluginName = id;				self.PluginName = id;				return false;			}		});		return PluginName;	};	this.start = function(name){		if(typeof this[name] !== "undefined"){			if(this.injecteds[name]){				$$("Script '" + name + "' injected into head");				var s = document.createElement('script');				s.type = 'text/javascript';				s.innerText = '(' + this[name].toString() + ').call(window)';				document.getElementsByTagName('head')[0].appendChild(s);			}else{				$$("Script '" + name + "' loaded");				this[name].call(this,[]);				}		}else{			$$("Plugin no definido...", name);		}	}}